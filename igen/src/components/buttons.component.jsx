import { useState } from "react";


const Buttons = ({button}) => {

  return (
            <div className="buttons">
            {this.state.types.map((type) => type)}
            </div>
  )
}

export default Buttons;